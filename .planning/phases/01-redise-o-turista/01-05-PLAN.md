---
phase: 01-redise-o-turista
plan: 05
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - frontend/lib/features/turista/home/presentation/screens/itinerary_screen.dart
  - frontend/lib/features/turista/home/presentation/widgets/itinerary_event_card.dart
  - frontend/lib/features/turista/home/presentation/screens/trip_home_screen.dart
  - frontend/lib/features/turista/home/presentation/screens/main_shell_screen.dart
  - frontend/lib/features/turista/home/presentation/screens/activity_detail_screen.dart
  - frontend/test/features/turista/home/presentation/widgets/itinerary_event_card_test.dart
autonomous: true
requirements: [DISENO-TUR-01]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The itinerary empty state routes through EmptyStateWidget with the existing Spanish copy 'No hay eventos planificados.' preserved verbatim, replacing the bare centred Text (UI-SPEC empty state, Itinerario list)."
    - "The itinerary error state renders the bloc-supplied state.message inside a dangerSoft banner with danger-token text, replacing the bare centred Text — and the message content itself is untouched (UI-SPEC error state, Itinerario load failure)."
    - "The itinerary loading spinner renders terracota rather than the Flutter default."
    - "An itinerary event title or description of any length wraps and grows the card vertically rather than clipping — no maxLines is introduced (UI-SPEC long-text, Itinerary event title/description)."
    - statement: "With 5+ itinerary items on screen, the new 16px radii, warm shadows and card margins preserve vertical rhythm without shadow overlap between adjacent cards."
      verification: backstop
    - "Activity status colors map to semantic tokens — finished to safe, in-progress to warn, pending to textMuted — instead of raw Material green/orange/grey, so status reads consistently with the rest of the app."
    - "The bottom navigation bar's selected item is terracota, its unselected items are the muted warm token, and its shadow is warm-tinted rather than neutral."
    - "activity_detail_screen.dart no longer imports app_colors.dart, and no file in this plan references AppBorderRadius or AppTextStyles — both belong to the Agencia scale (D-01)."
    - "Trip loading, filtering, navigation and activity-status logic are unchanged; only presentation moves."
  artifacts:
    - frontend/test/features/turista/home/presentation/widgets/itinerary_event_card_test.dart
  key_links:
    - "itinerary_screen.dart is the canonical BLoC state-branch pattern (loading/error/empty/loaded) that plans 01-06, 01-07 and 01-08 mirror — restyle its four branches carefully, because they become the reference."
    - "main_shell_screen.dart hosts TripHomeScreen, ChatScreen, HerramientasScreen, ConfigScreen and ComunicacionSeguridadScreen as tabs. Its bottom bar is the one chrome element visible on every primary Turista screen, so a miss here is visible in every pitch shot."
    - "trip_home_screen.dart pushes ActivityDetailScreen via Navigator.push with a plain MaterialPageRoute — that route inherits the app theme, so no per-route theme wiring is needed."
  prohibitions:
    - statement: "MUST NOT alter, translate or shorten the existing Spanish and localised copy on these screens — including 'No hay eventos planificados.', '¡Aviso Importante!', the activity status labels from l10n, and the filter chip labels. Only typography, color and container chrome change."
      category: transparency
      status: resolved
      verification: judgment
    - statement: "MUST NOT collapse the activity status distinction into a single accent. Finished, in-progress and pending must stay visually separable without relying on hue alone — each keeps its distinct icon alongside its token color, so the status remains readable to a colour-blind traveller."
      category: accessibility
      status: resolved
      verification: judgment
  assumptions:
    - "FLAGGED (UI-SPEC ## UI Considerations, populated / Itinerary list at typical volume, status 🧪 backstop): no automated evidence exists that 5+ cards keep their vertical rhythm under the new radii and shadows. Carried as a backstop truth above; it abstains rather than passing silently at verify time and is routed to the human check in plan 01-10."
    - "FLAGGED: trip_home_screen.dart is 1082 lines with 28 literal-colour sites — by far the largest single file in this phase. If Task 2 approaches its context budget, stop and record which sections remain rather than skimming the tail of the file; an unfinished section recorded honestly is recoverable, a silently skipped one is not."
---

<objective>
Restyle the Turista home surface — the trip home screen, the tab shell that frames every primary
screen, the itinerary list and its card, and the activity detail screen — onto the warm token system.

Purpose: this is the largest cluster of screens in the phase and the one a viewer sees first and
longest. It also contains the canonical BLoC state-branch pattern that three later plans copy.

Output: five restyled files, one card test, and a state-branch treatment the rest of the phase reuses.
</objective>

<execution_context>
@/home/the_julian_alva/Documentos/Projects/othliani/.claude/gsd-core/workflows/execute-plan.md
@/home/the_julian_alva/Documentos/Projects/othliani/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-redise-o-turista/01-CONTEXT.md
@.planning/phases/01-redise-o-turista/01-UI-SPEC.md
@.planning/phases/01-redise-o-turista/01-PATTERNS.md
@.planning/phases/01-redise-o-turista/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Itinerario — ramas de estado y tarjeta de evento</name>

  <files>
    frontend/lib/features/turista/home/presentation/screens/itinerary_screen.dart,
    frontend/lib/features/turista/home/presentation/widgets/itinerary_event_card.dart,
    frontend/test/features/turista/home/presentation/widgets/itinerary_event_card_test.dart
  </files>

  <read_first>
    - `frontend/lib/features/turista/home/presentation/screens/itinerary_screen.dart` — 68 lines, full file. The four state branches are the `if/else if` chain at lines 37-61.
    - `frontend/lib/features/turista/home/presentation/widgets/itinerary_event_card.dart` — 64 lines, full file. It is already partly theme-aware (`theme.colorScheme.primaryContainer`, `theme.textTheme.titleMedium`); the literal sites are the time-pill text style at lines 37-41.
    - `frontend/lib/core/widgets/empty_state_widget.dart` — as rewritten in 01-01; constructor takes `icon` and `message`.
    - `frontend/lib/core/theme/veltur_tokens.dart` — `danger`, `dangerSoft`, `primarySoft`, `textMuted`, `border`, `radiusMd`, `radiusFull`, `shadowSm`.
    - `.planning/phases/01-redise-o-turista/01-UI-SPEC.md` — the empty, error, long-text and populated rows of the UI Considerations table all target this pair of files.
  </read_first>

  <behavior>
    - Test 1: `ItineraryEventCard` renders its time pill on the `primarySoft` token with a full radius, and its label at the Label type role.
    - Test 2: a 300-character description renders in full with no ellipsis and no `maxLines` constraint, and the card's height grows to accommodate it.
    - Test 3: five stacked `ItineraryEventCard` widgets render without any card's rendered bounds overlapping the next — the mechanical half of the populated-volume backstop.
  </behavior>

  <action>
    **`itinerary_screen.dart` — the four state branches.** This is the reference treatment three later
    plans copy, so make each branch explicit rather than clever. Keep the `if (state is X)` structure
    exactly as it is; change only what each branch renders.
    - Loading: give the `CircularProgressIndicator` `color: Theme.of(context).colorScheme.primary`.
    - Error: replace the bare `Center(child: Text(state.message))` with a centred `Container` washed in
      the `dangerSoft` token, on the `radiusMd` (16) corner, padded `AppSpacing.lg`, containing an
      error icon in the `danger` token and the unmodified `state.message` at `textTheme.bodyLarge`
      colored with the text token. The message expression itself is not touched — it stays whatever the
      bloc produced.
    - Empty: replace the bare `Center(child: Text('No hay eventos planificados.'))` with
      `EmptyStateWidget(icon: Icons.event_busy_outlined, message: 'No hay eventos planificados.')`.
      The Spanish string is preserved character for character.
    - Loaded: keep the `ListView.builder`, its `AppSpacing.md` padding and the item mapping exactly.
    - Also remove the `Stack` wrapper around the `BlocBuilder` if it has no second child — it currently
      wraps a single child and adds a layout layer for nothing. Verify there is no second child before
      removing; if there is, leave the `Stack` in place.

    **`itinerary_event_card.dart`.** Replace the time pill's local `fontSize: 12` /
    `FontWeight.bold` literal with `theme.textTheme.labelLarge`, per the UI-SPEC Label role
    (14/600) — the pill's `minWidth: 48` constraint and `BoxShape.circle` accommodate the slightly
    larger type. Move its fill from `primaryContainer` to the `primarySoft` token and its text to the
    text token, so the pill reads as the palette's named soft-terracota wash rather than a Material
    container role. The `Card` itself inherits `radiusMd` (16) and `elevation: 0` from the theme, so
    add `shadowSm` explicitly — wrap the `Card` content in a `Container` with the token shadow, or set
    the `Card`'s `shape` and let a surrounding `DecoratedBox` carry the shadow; choose whichever keeps
    the `Card` semantics and record the choice in the SUMMARY. The `location_on` icon keeps
    `colorScheme.primary`. Do NOT add `maxLines` or `overflow` to the title or description — the
    UI-SPEC requires them to wrap and grow.

    **Import hygiene.** After these edits both files may still import `app_constants.dart` for
    `AppSpacing`, which stays legitimate this phase. Neither may reference `AppBorderRadius` or
    `AppTextStyles`.

    **Tests.** Create `itinerary_event_card_test.dart` with the three behaviors. For Test 3, pump five
    cards in a `Column` inside a sized box and compare each card's `tester.getRect(...)` bottom against
    the next card's top.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; flutter test test/features/turista/home/presentation/widgets/itinerary_event_card_test.dart</automated>
    <automated>cd frontend &amp;&amp; flutter analyze 2>&amp;1 | grep -cE '^\s+error •' | grep -qx 0</automated>
  </verify>

  <acceptance_criteria>
    - `cd frontend && flutter test test/features/turista/home/presentation/widgets/itinerary_event_card_test.dart` exits 0 with 3+ passing tests.
    - `cd frontend && grep -c 'No hay eventos planificados.' lib/features/turista/home/presentation/screens/itinerary_screen.dart` is exactly 1 — copy preserved verbatim.
    - `cd frontend && grep -c 'EmptyStateWidget' lib/features/turista/home/presentation/screens/itinerary_screen.dart` is at least 2 (import plus usage).
    - <!-- planner-discipline-allow: maxLines -->
      `cd frontend && ! grep -q 'maxLines' lib/features/turista/home/presentation/widgets/itinerary_event_card.dart` succeeds — long text still wraps rather than truncating.
    - <!-- planner-discipline-allow: AppBorderRadius -->
      <!-- planner-discipline-allow: AppTextStyles -->
      `cd frontend && ! grep -qE 'AppBorderRadius|AppTextStyles' lib/features/turista/home/presentation/screens/itinerary_screen.dart lib/features/turista/home/presentation/widgets/itinerary_event_card.dart` succeeds.
    - `git diff HEAD -- frontend/lib/features/turista/home/presentation/screens/itinerary_screen.dart | grep -cE '^[-+].*(ItineraryBloc|LoadItinerary|state\.items|sl<)'` is 0 — bloc wiring untouched.
    - `cd frontend && flutter analyze` reports zero lines matching `error •`.
  </acceptance_criteria>

  <done>
    An empty itinerary shows a warm teal icon over the existing Spanish line; a failed load shows a soft
    pink banner carrying the bloc's own message; a populated list shows soft-terracota time pills on
    16px cards with gentle warm shadows and no clipped descriptions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Pantalla principal del viaje (trip_home_screen)</name>

  <files>
    frontend/lib/features/turista/home/presentation/screens/trip_home_screen.dart
  </files>

  <read_first>
    - `frontend/lib/features/turista/home/presentation/screens/trip_home_screen.dart` — 1082 lines. Read it in sections rather than one pass. The literal-colour sites cluster at: the aviso dialog (~lines 92-108), the hero/trip-card block (~270-440), the status-badge row (~620-640), the `_buildStatusBadge` helper (783-800), the `_buildFilterChip` helper (802-831), the `ActivityCard` status switch (~855-871) and the `ActivityCard` container (~873-1010).
    - `frontend/lib/core/theme/veltur_tokens.dart` — `safe`, `safeSoft`, `warn`, `warnSoft`, `danger`, `dangerSoft`, `primarySoft`, `surfaceWarm`, `border`, `textMuted`, `radiusSm`, `radiusMd`, `radiusLg`, `radiusXl`, `radiusFull`, `shadowSm`, `shadowMd`.
    - `frontend/lib/features/turista/home/presentation/screens/itinerary_screen.dart` — as restyled in Task 1; its state-branch treatment is the reference for any bloc branch on this screen.
    - `.planning/phases/01-redise-o-turista/01-UI-SPEC.md` — the Color role table; note the accent is reserved for primary CTAs and must NOT be used for decorative backgrounds.
  </read_first>

  <action>
    Work section by section, top to bottom, committing nothing until `flutter analyze` is clean. Change
    presentation only: leave `TripBloc` interaction, `TripFilterChanged` dispatch, the location
    permission flow, `Navigator.push` targets and every `l10n` reference exactly as they are.

    **Status semantics — the highest-value change on this screen.** The `ActivityCard` status switch
    currently assigns raw Material colours: finished takes green, in-progress takes orange, pending
    takes grey. Map these to the semantic tokens: finished takes `safe`, in-progress takes `warn`,
    pending takes `textMuted`. Keep each status's distinct icon (`check_circle`, `play_circle`,
    `circle_outlined`) alongside the colour — the second prohibition on this plan requires status to
    stay readable without relying on hue. Keep the `statusLabel` values coming from `l10n` untouched.

    **`_buildStatusBadge`.** It takes a `Color` parameter and derives a 10%-alpha fill and a 30%-alpha
    border from it — that derivation pattern is fine and should stay, since it now receives token
    colours. Move its `borderRadius: 12` literal to `radiusSm` (10) and its local `fontSize: 12` /
    `FontWeight.w600` to `textTheme.labelLarge`. It currently takes no `BuildContext`; add one so it
    can reach the theme, and update its three call sites.

    **`_buildFilterChip`.** Already theme-derived. Replace the local `fontSize: 12` with
    `textTheme.labelLarge` and let the selected/unselected weight distinction stand — it uses w600 and
    normal, both already within the two-weight rule. Give the chip a `shape` with `radiusFull`, per the
    UI-SPEC pill assignment.

    **`ActivityCard` container.** Move both `AppBorderRadius.md` references (the `InkWell` splash
    radius and the container corner) to `radiusMd` (16). Replace the hand-rolled neutral shadow with
    `shadowSm`. Keep the in-progress emphasis (a 2px status-tinted border versus a 1px divider border)
    — that is a legible state distinction the redesign should preserve, with `theme.dividerColor` now
    resolving to the warm `border` token via the theme's `dividerTheme`.

    **Aviso dialog.** Its amber wash, orange icon, orange title and orange confirm button become the
    `warnSoft` and `warn` tokens. The Spanish string `¡Aviso Importante!` and the dialog body copy are
    preserved verbatim. Give the dialog a `shape` with `radiusLg` (24).

    **Hero and trip-card block.** This section carries the largest cluster of literal whites, neutral
    scrims and accent colours over a photographic background. Where white text sits on an image, keep
    it white — that is a legibility requirement, not a palette artefact. Replace neutral scrims and
    drop shadows with the warm-tinted token equivalents, replace the yellow rating accent with the
    `warn` token, replace the green availability accent with `safe`, and move every corner literal to
    the token scale — the hero surface takes `radiusXl` (32) per the UI-SPEC hero assignment, inner
    cards take `radiusMd` (16).

    **Weights.** Drop local `FontWeight.bold` overrides in favour of the theme's type roles wherever
    the surrounding text already resolves from `theme.textTheme`.

    **Budget honesty.** If context runs short before the file is finished, stop, commit what is clean,
    and record in the SUMMARY exactly which line ranges remain. Do not skim the remainder.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; flutter analyze 2>&amp;1 | grep -cE '^\s+error •' | grep -qx 0</automated>
    <automated>cd frontend &amp;&amp; ! (grep -hvE '^\s*(//|/\*|\*)' lib/features/turista/home/presentation/screens/trip_home_screen.dart | grep -qE 'Colors\.(red|pink|purple|deepPurple|indigo|blue|lightBlue|cyan|teal|green|lightGreen|lime|yellow|amber|orange|deepOrange|brown|grey|blueGrey|black)|Color\(0x')</automated>
  </verify>

  <acceptance_criteria>
    - <!-- planner-discipline-allow: Colors\.(red|pink|purple|deepPurple|indigo|blue|lightBlue|cyan|teal|green|lightGreen|lime|yellow|amber|orange|deepOrange|brown|grey|blueGrey|black) -->
      <!-- planner-discipline-allow: Color\(0x -->
      `cd frontend && ! (grep -hvE '^\s*(//|/\*|\*)' lib/features/turista/home/presentation/screens/trip_home_screen.dart | grep -qE 'Colors\.(red|pink|purple|deepPurple|indigo|blue|lightBlue|cyan|teal|green|lightGreen|lime|yellow|amber|orange|deepOrange|brown|grey|blueGrey|black)|Color\(0x')` succeeds. `Colors.white` and `Colors.transparent` stay permitted — both are intentional over photographic backgrounds.
    - <!-- planner-discipline-allow: AppBorderRadius -->
      `cd frontend && ! grep -q 'AppBorderRadius' lib/features/turista/home/presentation/screens/trip_home_screen.dart` succeeds.
    - `cd frontend && grep -c '¡Aviso Importante!' lib/features/turista/home/presentation/screens/trip_home_screen.dart` is exactly 1.
    - `cd frontend && grep -cE 'Icons\.(check_circle|play_circle|circle_outlined)' lib/features/turista/home/presentation/screens/trip_home_screen.dart` is at least 3 — each activity status keeps its distinct icon alongside its token colour.
    - `git diff HEAD -- frontend/lib/features/turista/home/presentation/screens/trip_home_screen.dart | grep -cE '^[-+].*(TripBloc|TripFilterChanged|Navigator\.push|Geolocator|LocationPermission|l10n\.)'` is 0 — bloc, navigation, permission and localisation paths untouched.
    - `cd frontend && flutter analyze` reports zero lines matching `error •`.
  </acceptance_criteria>

  <done>
    The trip home reads warm end to end: a soft-cornered hero, terracota filter pills, activity cards
    whose status shows as green-safe, amber-in-progress or muted-pending with their own icons, and an
    amber advisory dialog carrying the same Spanish warning as before.
  </done>
</task>

<task type="auto">
  <name>Task 3: Shell de pestañas y detalle de actividad</name>

  <files>
    frontend/lib/features/turista/home/presentation/screens/main_shell_screen.dart,
    frontend/lib/features/turista/home/presentation/screens/activity_detail_screen.dart
  </files>

  <read_first>
    - `frontend/lib/features/turista/home/presentation/screens/main_shell_screen.dart` — 135 lines, full file. It already reads `Theme.of(context).bottomNavigationBarTheme` and `Theme.of(context).primaryColor`; the one literal site is the bar's `shadow` at line ~113. The `_getTitle` switch at lines 119-134 carries l10n keys plus one hardcoded Spanish title.
    - `frontend/lib/features/turista/home/presentation/screens/activity_detail_screen.dart` — 144 lines, full file. It imports `app_colors.dart` and uses `AppColors.primary` at lines 42, 49, 100 and 138, plus neutral greys at 31, 34, 77, 95 and 105.
    - `frontend/lib/core/theme/veltur_tokens.dart` — `surfaceWarm`, `border`, `textMuted`, `radiusMd`, `radiusSm`, `radiusLg`, `shadowSm`, `shadowMd`.
    - `frontend/lib/core/theme/turista_theme.dart` — the `bottomNavigationBarTheme` and `textTheme` roles these two files inherit.
  </read_first>

  <action>
    **`main_shell_screen.dart`.** Replace the bottom bar's neutral drop shadow with the warm tint read
    through `VelturTokens.of(context)`, keeping the existing upward offset and blur so the bar's lift
    is unchanged. Move `leftCornerRadius` and `rightCornerRadius` from 16 to `radiusLg` (24), matching
    the UI-SPEC assignment for elevated panels — the bar is the most persistent chrome in the app and a
    16px corner would read as the odd one out against 24px sheets and buttons. The `activeIndex`
    handling, the `onTap` state update, the tab widget list and the `_getTitle` switch stay exactly as
    they are; the hardcoded Spanish title `Comunicación y Seguridad` is preserved verbatim, including
    its inconsistency with the surrounding l10n keys — normalising it is a copy change this phase does
    not make.

    **`activity_detail_screen.dart`.** Remove the `app_colors.dart` import entirely — this is one of
    eight Turista files still reaching into the Agencia navy palette, and D-01 removes that path.
    - Every `AppColors.primary` becomes `Theme.of(context).colorScheme.primary`.
    - The image placeholder's neutral fill becomes the `surfaceWarm` token and its placeholder icon the
      `textMuted` token; its corner moves from `AppBorderRadius.md` to `radiusMd` (16).
    - The location callout's neutral fill becomes `surfaceWarm` with a `border`-token outline; its
      corner moves from `AppBorderRadius.sm` to `radiusSm` (10).
    - Every neutral grey body text becomes `textMuted`.
    - Replace the five local `fontSize`/`FontWeight.bold` literals with theme roles: the 24px title
      takes `textTheme.headlineSmall`, the three 18px section headings take `textTheme.titleLarge`, and
      the 14-16px body lines take `textTheme.bodyLarge` or `labelLarge` as appropriate.
    - Keep every Spanish string and the `activityTitle` / `activityTime` / `activityDescription`
      constructor parameters exactly as they are.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; flutter analyze 2>&amp;1 | grep -cE '^\s+error •' | grep -qx 0</automated>
    <automated>cd frontend &amp;&amp; ! grep -q 'theme/app_colors.dart' lib/features/turista/home/presentation/screens/activity_detail_screen.dart</automated>
  </verify>

  <acceptance_criteria>
    - <!-- planner-discipline-allow: theme/app_colors.dart -->
      `cd frontend && ! grep -q 'theme/app_colors.dart' lib/features/turista/home/presentation/screens/activity_detail_screen.dart` succeeds — the Agencia palette import is gone.
    - <!-- planner-discipline-allow: Colors\.(red|pink|purple|deepPurple|indigo|blue|lightBlue|cyan|teal|green|lightGreen|lime|yellow|amber|orange|deepOrange|brown|grey|blueGrey|black) -->
      <!-- planner-discipline-allow: Color\(0x -->
      `cd frontend && ! (grep -hvE '^\s*(//|/\*|\*)' lib/features/turista/home/presentation/screens/main_shell_screen.dart lib/features/turista/home/presentation/screens/activity_detail_screen.dart | grep -qE 'Colors\.(red|pink|purple|deepPurple|indigo|blue|lightBlue|cyan|teal|green|lightGreen|lime|yellow|amber|orange|deepOrange|brown|grey|blueGrey|black)|Color\(0x')` succeeds.
    - <!-- planner-discipline-allow: AppBorderRadius -->
      `cd frontend && ! grep -q 'AppBorderRadius' lib/features/turista/home/presentation/screens/activity_detail_screen.dart` succeeds.
    - `cd frontend && grep -c 'Comunicación y Seguridad' lib/features/turista/home/presentation/screens/main_shell_screen.dart` is exactly 1 — the hardcoded title is preserved verbatim.
    - `git diff HEAD -- frontend/lib/features/turista/home/presentation/screens/main_shell_screen.dart | grep -cE '^[-+].*(_currentIndex|activeIndex|onTap|_getTitle|l10n\.)'` is 0 — tab behaviour and titles untouched.
    - `cd frontend && flutter analyze` reports zero lines matching `error •`.
  </acceptance_criteria>

  <done>
    Every primary Turista screen is framed by a 24px-cornered warm bottom bar with a terracota selected
    tab and a soft warm lift, and the activity detail screen reads in warm type on warm surfaces with
    no remaining link to the navy palette.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device GPS → trip home | Pre-existing. `trip_home_screen.dart` requests location permission and reads position. Unchanged by this plan. |

No storage, auth, network endpoint or parsing surface changes. This plan edits colours, radii,
shadows and type roles.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05-T | Tampering | location-permission flow in `trip_home_screen.dart` | low | mitigate | A 1082-line restyle could drift into the permission request and change when or whether the app asks for GPS. Task 2's fifth acceptance criterion is a `git diff` gate asserting zero changed lines matching `Geolocator` and `LocationPermission`. |
| T-01-05-I | Information Disclosure | activity status legibility | low | mitigate | Collapsing three statuses onto one accent would misrepresent trip state to the traveller. Mitigated by keeping a distinct icon per status and asserting all three icon names survive in the file. |
| T-01-05-SC | Tampering | package installs | n/a | accept | This plan installs nothing. |
</threat_model>

<verification>
1. `cd frontend && flutter test test/features/turista/home/` — exits 0.
2. `cd frontend && flutter analyze` — zero `error •` lines.
3. Scoped literal-colour gate over all five modified files — passes.
4. `! grep -qE 'AppBorderRadius|AppTextStyles' <the five files>` — passes.
5. `! grep -q 'theme/app_colors.dart' activity_detail_screen.dart` — passes.
6. Copy-preservation greps for the four named Spanish strings — each returns exactly 1.
7. Human check deferred to 01-10: the populated-itinerary backstop (5+ cards, vertical rhythm).
</verification>

<success_criteria>
- The itinerary's four state branches are the phase's reference treatment, all four restyled.
- Activity status maps to semantic tokens while keeping per-status icons.
- The tab shell — visible behind every primary screen — carries warm chrome at the 24px radius.
- No file in this plan reaches into the Agencia palette or the Agencia radius scale.
- No bloc, navigation, permission or localisation line changed.
</success_criteria>

<artifacts_this_phase_produces>
| Symbol / file | Kind | Notes |
|---|---|---|
| `frontend/test/features/turista/home/presentation/widgets/itinerary_event_card_test.dart` | new file | 3+ tests incl. the populated-volume mechanical half |
| `_buildStatusBadge(BuildContext, String, Color)` | changed signature | Gains a `BuildContext` first parameter; three call sites updated |
| Second Turista call site of `EmptyStateWidget` | new wiring | `itinerary_screen.dart` |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-redise-o-turista/01-05-SUMMARY.md` when done. Record: how the `Card`
shadow was attached in `itinerary_event_card.dart`, whether the `Stack` in `itinerary_screen.dart` was
removable, and — if Task 2 ran short of context — the exact line ranges of `trip_home_screen.dart`
left unfinished.
</output>
