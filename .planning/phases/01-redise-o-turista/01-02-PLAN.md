---
phase: 01-redise-o-turista
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - frontend/lib/core/widgets/info_modal.dart
  - frontend/lib/core/widgets/saving_overlay.dart
  - frontend/lib/core/widgets/phone_number_field.dart
  - frontend/test/core/widgets/shared_widgets_theme_test.dart
autonomous: true
requirements: [DISENO-TUR-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "InfoModal, SavingOverlay and PhoneNumberField render every color, radius and shadow from Theme.of(context) / VelturTokens.of(context) — none carries a literal color of its own (D-02)."
    - "The same three widgets, pumped under a second VelturTokens instance, render different colors — proving Guia can reuse them in Phase 2 without editing them."
    - "SavingOverlay's spinner is the terracota primary, not the legacy blue, and its circle shadow is warm-tinted rather than neutral."
    - "InfoModal's sheet uses the 24px large radius on its top corners and the surface token for its background."
    - "PhoneNumberField no longer overrides the theme's input border, so it inherits the 10px small radius and terracota focus border from TuristaTheme."
    - "The country-picker bottom sheet opened from PhoneNumberField renders its search field with the same warm border treatment as the rest of the app, despite living in its own overlay widget tree."
    - "Public constructor signatures and static APIs of all three widgets are unchanged, so no call site in Turista, Guia or Agencia needs editing."
  artifacts:
    - frontend/test/core/widgets/shared_widgets_theme_test.dart
  key_links:
    - "PhoneNumberField is consumed by phone_screen.dart and InfoModal by phone_screen.dart + register_screen.dart (plan 01-09) — if this plan changes a constructor signature, plan 01-09 breaks."
    - "All three widgets must resolve tokens through VelturTokens.of(context), the idiom fixed in 01-01 — a direct TuristaColors import here would silently un-do D-02."
  prohibitions:
    - statement: "MUST NOT change the constructor signature, static API surface, animation timing or dismissal behaviour of these shared widgets — they are consumed by Agencia and Guia call sites that this phase does not verify, and a signature change would break apps outside the phase boundary."
      category: safety
      status: resolved
      verification: judgment
    - statement: "MUST NOT weaken SavingOverlay's blocking contract (PopScope canPop false, barrierDismissible false) while restyling — the overlay exists to prevent double-submits during a save, and a purely visual pass must not turn a guard into decoration."
      category: safety
      status: resolved
      verification: judgment
  assumptions:
    - "FLAGGED: SavingOverlay currently has ZERO call sites in the Turista tree (grep over lib/ excluding guia/agencia returns none). It is restyled anyway because D-02 scopes it explicitly and Phase 2 is expected to use it. Its restyle therefore cannot be visually confirmed by navigating Turista — only the widget test proves it."
    - "FLAGGED: the country-picker sheet is rendered by the third-party `country_picker` package in its own overlay route. Its theming is limited to what CountryListThemeData exposes; if the package does not surface a hook for some element, that element stays package-default and is recorded as such in the SUMMARY rather than worked around."
---

<objective>
Finish the D-02 generalization of `frontend/lib/core/widgets/` — the three shared widgets that plan
01-01 did not cover — so that every widget in that directory is theme-driven and reusable by Guía in
Phase 2 without modification.

Purpose: `core/widgets/` is the one directory both apps share. If a widget there keeps a literal
color, Phase 2 must either fork it or accept a navy artefact in a warm app. This plan removes that
choice.

Output: three token-driven widgets and one test file that holds the line.
</objective>

<execution_context>
@/home/the_julian_alva/Documentos/Projects/othliani/.claude/gsd-core/workflows/execute-plan.md
@/home/the_julian_alva/Documentos/Projects/othliani/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-redise-o-turista/01-CONTEXT.md
@.planning/phases/01-redise-o-turista/01-UI-SPEC.md
@.planning/phases/01-redise-o-turista/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SavingOverlay e InfoModal leen sus tokens del tema</name>

  <files>
    frontend/lib/core/widgets/saving_overlay.dart,
    frontend/lib/core/widgets/info_modal.dart,
    frontend/test/core/widgets/shared_widgets_theme_test.dart
  </files>

  <read_first>
    - `frontend/lib/core/widgets/saving_overlay.dart` — 109 lines, full file. Note the static `show`/`hide`/`showAndWait` API and the `PopScope(canPop: false)` guard, both of which must survive unchanged.
    - `frontend/lib/core/widgets/info_modal.dart` — 189 lines, full file. Note the `_fade`/`_slide` animation controllers (lines ~61-82) and the `titleColor` constructor parameter.
    - `frontend/lib/core/theme/veltur_tokens.dart` — the field names to read (created in 01-01).
    - `frontend/lib/core/theme/turista_theme.dart` — the component-theme defaults these widgets inherit.
    - `frontend/test/core/widgets/empty_state_widget_test.dart` — the portability-test idiom established in 01-01; mirror its structure rather than inventing a second one.
    - `.planning/phases/01-redise-o-turista/01-UI-SPEC.md` — Radius & Shadow Tokens table.
  </read_first>

  <behavior>
    - Test 1: `SavingOverlay` pumped under `TuristaTheme.lightTheme` renders a `CircularProgressIndicator` whose color is the terracota primary.
    - Test 2: `SavingOverlay`'s spinner-circle `BoxShadow` color carries the warm brown tint, not a neutral.
    - Test 3: `InfoModal`'s sheet container uses a top `BorderRadius` of 24 and the surface color token.
    - Test 4: `InfoModal` pumped under a second `VelturTokens` instance renders a different drag-handle color than under the Turista instance.
    - Test 5: `SavingOverlay.show` still opens a barrier that cannot be dismissed by tapping outside, and the widget still refuses a back-pop.
  </behavior>

  <action>
    **`saving_overlay.dart`.** Convert the four literal styling values to theme lookups, keeping the
    `PopScope`, the `BackdropFilter` blur sigmas, the `Dialog(backgroundColor: transparent)` and every
    static method byte-identical in behaviour.
    - The spinner circle's background becomes `Theme.of(context).colorScheme.surface`.
    - Its `boxShadow` becomes `VelturTokens.of(context).shadowMd`, replacing the hand-rolled neutral
      shadow — this is the single clearest hardcoded-to-token migration in the shared set and the
      reference the other two widgets follow.
    - The spinner's color becomes `Theme.of(context).colorScheme.primary`, replacing the legacy blue
      ARGB literal. Delete the trailing comment that names the old blue.
    - The `mensaje` text style becomes `Theme.of(context).textTheme.titleMedium` recolored to the
      surface token, keeping the existing weight-600 emphasis and letter spacing; replace its neutral
      drop `Shadow` with one tinted by `TuristaColors.shadowTint` reached through the token extension
      (use the first entry of `shadowMd` as the tint source so no new literal is introduced).
    - The barrier in `show()` is a static method with no `BuildContext` theme available at the point
      the color is computed — it does have `context`, so read the warm tint via
      `VelturTokens.of(context)` there and build the barrier color from it at 35% alpha, preserving the
      existing opacity.

    **`info_modal.dart`.** Replace the literal chrome with tokens while leaving the animation block
    (controllers, curves, durations) and the dismissal logic untouched — those are established
    interaction affordances, not part of the visual redesign scope.
    - The sheet `Container` background becomes `Theme.of(context).colorScheme.surface` and its
      `BorderRadius.vertical(top:)` becomes `VelturTokens.of(context).radiusLg` (24), up from the
      current 16. Note the decoration is currently `const` — it can no longer be, since it now depends
      on context.
    - The `showModalBottomSheet` shape at the top of the file gets the same 24 top radius so the sheet
      and its clip agree; keep its transparent background, which is intentional for rounded corners.
    - The drag-handle pill takes `VelturTokens.of(context).border` as its color.
    - The leading `Icon`'s default (when `titleColor` is null) becomes
      `Theme.of(context).colorScheme.primary` instead of a neutral — an info sheet's icon is an accent
      element per the UI-SPEC color roles.
    - The title `Text` already reads `theme.textTheme.titleLarge`; keep that and drop the local
      `fontWeight: FontWeight.w700` override so the two-weight rule from the UI-SPEC holds — `titleLarge`
      already carries weight 600 from `TuristaTheme`.
    - The `Cerrar` `TextButton` keeps its exact Spanish label; it inherits its color from the theme.

    **Tests.** Create `shared_widgets_theme_test.dart` with the five behaviors above, following the
    project's AAA structure, Spanish test names and `t`-prefixed constants.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; flutter test test/core/widgets/shared_widgets_theme_test.dart</automated>
    <automated>cd frontend &amp;&amp; flutter analyze 2>&amp;1 | grep -cE '^\s+error •' | grep -qx 0</automated>
  </verify>

  <acceptance_criteria>
    - `cd frontend && flutter test test/core/widgets/shared_widgets_theme_test.dart` exits 0 with 5+ passing tests.
    - <!-- planner-discipline-allow: Colors\.(red|pink|purple|deepPurple|indigo|blue|lightBlue|cyan|teal|green|lightGreen|lime|yellow|amber|orange|deepOrange|brown|grey|blueGrey|black) -->
      <!-- planner-discipline-allow: Color\(0x -->
      `cd frontend && ! (grep -hvE '^\s*(//|/\*|\*)' lib/core/widgets/saving_overlay.dart lib/core/widgets/info_modal.dart | grep -qE 'Colors\.(red|pink|purple|deepPurple|indigo|blue|lightBlue|cyan|teal|green|lightGreen|lime|yellow|amber|orange|deepOrange|brown|grey|blueGrey|black)|Color\(0x')` succeeds. `Colors.transparent` and `Colors.white` remain permitted — both are intentional in these two files.
    - `git diff HEAD -- frontend/lib/core/widgets/saving_overlay.dart | grep -cE '^[-+].*(static void show|static Future<void> showAndWait|static void hide|canPop|barrierDismissible)'` is 0 — the static API and the blocking guard are untouched.
    - `git diff HEAD -- frontend/lib/core/widgets/info_modal.dart | grep -cE '^[-+].*(Duration|Curve|AnimationController|_fade|_slide)'` is 0 — animation behaviour is untouched.
    - `cd frontend && flutter analyze` reports zero lines matching `error •`.
  </acceptance_criteria>

  <done>
    Opening any info sheet shows a warm white sheet with 24px top corners and a terracota icon; any
    saving overlay shows a terracota spinner inside a warm-shadowed circle. Neither widget contains a
    color of its own, and both still block interaction exactly as before.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PhoneNumberField hereda el tema en vez de sobrescribirlo</name>

  <files>
    frontend/lib/core/widgets/phone_number_field.dart,
    frontend/test/core/widgets/shared_widgets_theme_test.dart
  </files>

  <read_first>
    - `frontend/lib/core/widgets/phone_number_field.dart` — 166 lines, full file. The two `OutlineInputBorder` sites are at roughly line 97 (inside `_pickCountry`'s `CountryListThemeData`) and line 128 (the main `TextFormField` decoration).
    - `frontend/lib/core/theme/turista_theme.dart` — the `inputDecorationTheme` this widget must inherit (10px radius, warm border, terracota focus).
    - `frontend/test/core/widgets/shared_widgets_theme_test.dart` — extend the file created in Task 1.
  </read_first>

  <behavior>
    - Test 6: `PhoneNumberField` pumped under `TuristaTheme.lightTheme` renders a `TextFormField` whose resolved input border radius is 10, inherited from the theme rather than declared locally.
    - Test 7: focusing the field turns its border terracota (the theme's `focusedBorder`), not the Material default.
    - Test 8: the widget's `onChanged` still emits the same masked/unmasked value pair it emits today (a behaviour guard, not a visual assertion).
  </behavior>

  <action>
    **Main field.** Delete the local `border: const OutlineInputBorder()` from the `TextFormField`'s
    `InputDecoration`. Today this bare override shadows `TuristaTheme.inputDecorationTheme`, so the
    field would keep Material's default 4px corners and default focus color while every other input in
    the app moves to the 10px warm treatment. Removing the line — rather than restating the theme
    values locally — is the point: the field must inherit. Keep `hintText`, `prefixIcon`,
    `keyboardType`, `autofillHints`, `inputFormatters` and `onChanged` exactly as they are.

    **Hint text.** Give the hint the muted token via
    `hintStyle: Theme.of(context).textTheme.bodyLarge?.copyWith(color: VelturTokens.of(context).textMuted)`
    so the placeholder digits read as warm-muted rather than Material grey. Keep the literal example
    number string unchanged.

    **Country-picker sheet.** The `showCountryPicker` overlay is a separate widget tree that does not
    inherit the calling screen's `InputDecorationTheme`, so here the values must be stated explicitly
    rather than inherited. Give its `CountryListThemeData` a `borderRadius` and `inputDecoration` built
    from the tokens: `OutlineInputBorder` at `radiusSm` (10) with a `border`-token side, a focused
    variant in the primary color, `filled: true` on the surface token, and the sheet's own
    `borderRadius` at `radiusLg` (24) to match `InfoModal`. Also pass `textStyle` and
    `searchTextStyle` from `Theme.of(context).textTheme` so the picker uses Poppins rather than the
    package default. Keep `showPhoneCode: true`, the `favorite` list `['MX', 'US', 'ES']`, the
    `bottomSheetHeight: 600` and the Spanish label `Buscar país` verbatim.

    Read the `country_picker` 2.0.26 `CountryListThemeData` field list before writing — if a field
    named here does not exist in that version, use the nearest equivalent it does expose and record
    the substitution in the SUMMARY rather than adding a wrapper.

    **Tests.** Append the three behaviors above to `shared_widgets_theme_test.dart`.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; flutter test test/core/widgets/</automated>
    <automated>cd frontend &amp;&amp; flutter analyze 2>&amp;1 | grep -cE '^\s+error •' | grep -qx 0</automated>
  </verify>

  <acceptance_criteria>
    - `cd frontend && flutter test test/core/widgets/` exits 0 with 8+ passing tests across the directory.
    - <!-- planner-discipline-allow: border: const OutlineInputBorder() -->
      `cd frontend && ! grep -q 'border: const OutlineInputBorder()' lib/core/widgets/phone_number_field.dart` succeeds — the theme-shadowing override is gone from the main field.
    - `cd frontend && grep -c 'Buscar país' lib/core/widgets/phone_number_field.dart` is exactly 1 and `grep -c "'MX', 'US', 'ES'" lib/core/widgets/phone_number_field.dart` is exactly 1 — Spanish copy and favourites list preserved.
    - `git diff HEAD -- frontend/lib/core/widgets/phone_number_field.dart | grep -cE '^[-+].*(_maskFormatter|_emitChange|onChanged|localDigits|_flagEmoji)'` is 0 — input behaviour is untouched.
    - `cd frontend && flutter analyze` reports zero lines matching `error •`.
  </acceptance_criteria>

  <done>
    The phone field on the Turista phone screen renders with 10px warm-bordered corners and turns
    terracota on focus, and the country picker it opens reads as part of the same app rather than a
    Material default sheet.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → PhoneNumberField | An existing untrusted-input surface whose formatting/masking this plan must leave intact. |

No new boundary is introduced: this plan changes colors, radii and shadows on three widgets that
already exist. No storage, network, auth or parsing surface changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02-T | Tampering | `phone_number_field.dart` input masking | low | mitigate | A restyle that touched `_maskFormatter` or `_emitChange` could silently change what phone value reaches the auth flow. Task 2's fourth acceptance criterion is a `git diff` gate asserting zero changed lines touching the mask/emit path. |
| T-01-02-D | Denial of Service | `saving_overlay.dart` blocking guard | low | mitigate | Weakening `PopScope(canPop: false)` or `barrierDismissible: false` would let a user dismiss the overlay and double-submit a save. Task 1's third acceptance criterion is a `git diff` gate on exactly those tokens, and Test 5 asserts the behaviour. |
| T-01-02-SC | Tampering | package installs | n/a | accept | This plan installs nothing. `google_fonts` was the phase's only dependency addition and was verified in 01-01 (publisher `flutter.dev`). No package-legitimacy checkpoint applies here. |
</threat_model>

<verification>
1. `cd frontend && flutter test test/core/widgets/` — exits 0.
2. `cd frontend && flutter analyze` — zero `error •` lines.
3. Scoped literal-color gate over the three files (see Task 1 / Task 2 acceptance criteria) — passes.
4. `git diff HEAD --stat -- frontend/lib/core/widgets/` shows exactly three modified files.
</verification>

<success_criteria>
- All four widgets in `frontend/lib/core/widgets/` are theme-driven (one from 01-01, three from here).
- A portability test proves each renders differently under a second `VelturTokens` instance.
- No constructor signature, static API, animation or input-masking behaviour changed.
</success_criteria>

<artifacts_this_phase_produces>
| Symbol / file | Kind | Notes |
|---|---|---|
| `frontend/test/core/widgets/shared_widgets_theme_test.dart` | new file | 8+ tests across both tasks |

No new production symbols. All three widgets keep their existing public API by design — that
invariance is itself an acceptance criterion of this plan.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-redise-o-turista/01-02-SUMMARY.md` when done. Record any
`CountryListThemeData` field substitution made for `country_picker` 2.0.26, and note that
`SavingOverlay` has no Turista call site so its restyle is test-verified only.
</output>
