---
phase: 01-redise-o-turista
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - frontend/lib/core/tools/presentation/screens/currency_converter_screen.dart
autonomous: true
requirements: [DISENO-TUR-03, DISENO-TUR-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The converted-amount figure renders at the Display type role (32px, weight 600) in the terracota primary, inside a card washed with the primarySoft token on the 16px medium radius (DISENO-TUR-03)."
    - "Every radius on this screen comes from the Turista token scale, not from AppBorderRadius, which belongs to Agencia."
    - "The OCR full-screen loading scrim is warm-tinted rather than a neutral black wash, and both CircularProgressIndicator instances render terracota rather than the Flutter default (UI-SPEC loading state, Currency converter OCR processing)."
    - "The validation SnackBars keep the existing l10n strings invalidAmount and noNumberFound verbatim and render on the danger token background (UI-SPEC error state, Currency converter)."
    - "Currency selector dropdown item names keep their existing TextOverflow.ellipsis treatment, so a long currency name truncates rather than overflowing (UI-SPEC overflow state, Currency selector dropdown item names)."
    - "The conversion math, the exchange-rate fetch, the OCR pipeline, the image pickers and the CurrencyCubit state handling are unchanged — this plan touches only presentation."
    - "The source-currency and target-currency selectors remain individually labelled, so the direction of the conversion is unambiguous after the restyle."
  artifacts: []
  key_links:
    - "The screen reads state from CurrencyCubit via a BlocBuilder; the result card branches on CurrencyLoading. Restyling must preserve that branch, since it is the only loading affordance for the rate fetch."
    - "AppSpacing (4/8/16/24/32) stays in use unchanged — only AppBorderRadius is replaced. Do not swap spacing constants while replacing radius constants."
  prohibitions:
    - statement: "MUST NOT obscure which currency is the source and which is the target, and MUST NOT present a demo or cached exchange rate in a way that implies it is a live market rate. A traveller acts on this number with real money; a restyle that trades clarity for visual polish is a transparency failure."
      category: transparency
      status: resolved
      verification: judgment
    - statement: "MUST NOT alter the conversion arithmetic, the rate source, the rounding, or the currency symbol/name lookup while restyling. A visual pass that changes a displayed figure is indistinguishable from a bug to the user."
      category: safety
      status: resolved
      verification: judgment
    - statement: "MUST NOT alter, translate or shorten the existing Spanish and localised copy on this screen — including the l10n keys result, takePhoto, gallery, invalidAmount, noNumberFound and error. Only typography, color and container chrome change."
      category: transparency
      status: resolved
      verification: judgment
  assumptions:
    - "FLAGGED (edge-probe fallback, unresolved/unclassified): DISENO-TUR-03 came back `unclassified` from the deterministic edge probe — 'the currency converter reflects the new design system' has no boundary/null/permission edge to classify, so no acceptance criterion was auto-derived. The planner substitutes the Display-role and token-radius assertions above as the proxy; that substitution is an assumption, not a probe result."
    - "FLAGGED: the existing figure already renders at fontSize 32, matching the UI-SPEC Display role exactly. The only type change needed is weight — bold to 600 — plus routing it through textTheme.displaySmall so the role is named rather than restated. Recorded so a reviewer does not read the small diff as an omission."
---

<objective>
Deliver DISENO-TUR-03: the currency converter — the third screen the pitch video records — reads as
part of the warm system, with the conversion figure carrying the Display type role and every radius
moved off the Agencia scale, while the conversion, rate fetch and OCR pipeline stay untouched.

Purpose: this screen is already the most theme-aware in the Turista tree (it reads `theme.colorScheme`
throughout). That makes it the cheapest of the three demo screens to finish — and the easiest to
under-deliver on, because it looks nearly done already while still wearing 12px Agencia radii, a
neutral scrim and a default-blue spinner.

Output: one restyled screen, no behavioural diff.
</objective>

<execution_context>
@/home/the_julian_alva/Documentos/Projects/othliani/.claude/gsd-core/workflows/execute-plan.md
@/home/the_julian_alva/Documentos/Projects/othliani/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-redise-o-turista/01-CONTEXT.md
@.planning/phases/01-redise-o-turista/01-UI-SPEC.md
@.planning/phases/01-redise-o-turista/01-PATTERNS.md
@.planning/phases/01-redise-o-turista/01-01-SUMMARY.md
@.planning/sketches/003-conversor-divisas/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tarjeta de resultado, escala tipográfica y radios de token</name>

  <files>
    frontend/lib/core/tools/presentation/screens/currency_converter_screen.dart
  </files>

  <read_first>
    - `frontend/lib/core/tools/presentation/screens/currency_converter_screen.dart` — 525 lines. Read the whole `build` method and its helpers; the sites this task changes are the image-preview container (~lines 214-237), the amount `TextField` (~280-305), the swap `IconButton` (~324-340), the result card (~358-402), the rate-detail chip (~406-438) and the currency-selector helper (~470-510).
    - `frontend/lib/core/theme/veltur_tokens.dart` — `primarySoft`, `border`, `textMuted`, `surfaceWarm`, `radiusSm`, `radiusMd`, `radiusLg`, `radiusFull`, `shadowSm`.
    - `frontend/lib/core/theme/turista_theme.dart` — the `textTheme` roles (`displaySmall` 32/w600, `labelLarge` 14/w600, `bodyLarge` 16/w400) and the `inputDecorationTheme` this screen's fields inherit.
    - `frontend/lib/core/theme/app_constants.dart` — confirm `AppBorderRadius.md` is 12 and `.sm` is 8, i.e. the Agencia scale this screen currently wears.
    - `.planning/sketches/003-conversor-divisas/` — winning variant C, design direction only.
    - `.planning/phases/01-redise-o-turista/01-UI-SPEC.md` — Typography, Radius and Color tables.
  </read_first>

  <action>
    Restyle the screen's chrome. Do not touch `_convert`, the rate fetch, `_getCurrencySymbol`,
    `_getCurrencyName`, the OCR/`_processImage` path, `_takePicture`, `_pickFromGallery`, or any
    `CurrencyCubit` interaction.

    **Radii.** Replace every `AppBorderRadius` reference with the Turista token equivalent read from
    `VelturTokens.of(context)`: the image-preview container and its `ClipRRect` move from
    `AppBorderRadius.md` (12) to `radiusMd` (16); the result card moves from `AppBorderRadius.md` (12)
    to `radiusLg` (24), since the UI-SPEC assigns the large radius to elevated panels and this is the
    screen's hero surface; the rate-detail chip moves from `AppBorderRadius.sm` (8) to `radiusSm` (10).
    The image preview's `Border.all` keeps the primary color but drops to width 1 and takes the
    `border` token — a 2px terracota outline around a photo reads as an error state in the new palette.

    **Result card.** Replace the `primary.withValues(alpha: 0.1)` background with the `primarySoft`
    token, which is the palette's named value for exactly this wash. Reduce the 2px primary border to
    a 1px `border`-token side and add `shadowSm` — in the warm system, depth comes from a soft shadow
    rather than a heavy outline. The `l10n.result` caption takes `textTheme.labelLarge` recolored to
    `textMuted`. The figure itself takes `textTheme.displaySmall` recolored to
    `colorScheme.primary`, replacing the local `fontSize: 32` / `FontWeight.bold` literal — the size is
    already correct, so this changes weight from bold to 600 and names the role rather than restating
    it. Keep the `FittedBox(fit: BoxFit.scaleDown)` wrapper, which is what stops a large figure from
    overflowing. The currency-name line below takes `textTheme.bodyLarge` recolored to `textMuted`.

    **Direction clarity.** While restyling the two currency selectors, verify each still carries its
    own visible label distinguishing source from target. If the restyle would leave the two selectors
    visually symmetric with no direction cue, keep or restore the existing cue — this is the screen's
    transparency prohibition, and a matched pair of unlabelled dropdowns is exactly the ambiguity it
    forbids. Do not invent new copy to do this; reuse the labels already present.

    **Rate-detail chip.** Its `surfaceContainerHighest` background becomes the `surfaceWarm` token and
    its local `fontSize: 12` literal becomes `textTheme.labelLarge` recolored to `textMuted`.

    **Swap control.** The centre swap `IconButton` takes `colorScheme.primary` as its foreground on a
    `primarySoft` fill with `radiusFull`, making it read as the pill affordance the UI-SPEC assigns to
    circular controls. Keep its existing 56-64px touch target — the UI-SPEC explicitly exempts
    icon-only touch targets from the 8-point scale.

    **Amount field.** Remove any local border, fill or radius declaration so the field inherits the
    theme's `inputDecorationTheme` (10px, warm border, terracota focus). Keep the suffix
    `IconButton`, the keyboard type and the controller wiring exactly as they are.

    **Weights.** Drop every local `FontWeight.bold` on this screen in favour of the theme roles, per
    the UI-SPEC two-weight rule.

    **Spacing.** `AppSpacing` values stay exactly as they are — the UI-SPEC confirms the existing
    4/8/16/24/32 scale is already correct. Do not swap spacing constants while swapping radius
    constants.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; flutter analyze 2>&amp;1 | grep -cE '^\s+error •' | grep -qx 0</automated>
    <automated>cd frontend &amp;&amp; ! grep -q 'AppBorderRadius' lib/core/tools/presentation/screens/currency_converter_screen.dart</automated>
  </verify>

  <acceptance_criteria>
    - <!-- planner-discipline-allow: AppBorderRadius -->
      `cd frontend && ! grep -q 'AppBorderRadius' lib/core/tools/presentation/screens/currency_converter_screen.dart` succeeds — every radius now comes from the Turista token scale.
    - <!-- planner-discipline-allow: fontSize: 32 -->
      `cd frontend && ! grep -q 'fontSize: 32' lib/core/tools/presentation/screens/currency_converter_screen.dart` succeeds — the figure is expressed as the Display type role, not a local size literal.
    - `cd frontend && grep -c 'AppSpacing' lib/core/tools/presentation/screens/currency_converter_screen.dart` is unchanged from its pre-task value — spacing was not collaterally edited.
    - `cd frontend && grep -c 'TextOverflow.ellipsis' lib/core/tools/presentation/screens/currency_converter_screen.dart` is at least 4 — the existing overflow protection on button labels and currency names survived.
    - `git diff HEAD -- frontend/lib/core/tools/presentation/screens/currency_converter_screen.dart | grep -cE '^[-+].*(_convert|toStringAsFixed|_getCurrencySymbol|_getCurrencyName|_processImage|_takePicture|_pickFromGallery|CurrencyCubit|rates\[)'` is 0 — conversion, formatting and OCR paths untouched.
    - `cd frontend && flutter analyze` reports zero lines matching `error •`.
  </acceptance_criteria>

  <done>
    The converter shows the result figure at 32px/600 terracota inside a soft peach card with 24px
    corners and a warm shadow, the swap control reads as a terracota pill, and no radius on the screen
    still comes from the Agencia scale — while every converted number is identical to before.
  </done>
</task>

<task type="auto">
  <name>Task 2: Estados de carga y error del conversor en tokens cálidos</name>

  <files>
    frontend/lib/core/tools/presentation/screens/currency_converter_screen.dart
  </files>

  <read_first>
    - `frontend/lib/core/tools/presentation/screens/currency_converter_screen.dart` — as modified by Task 1. The five `SnackBar` sites are at roughly lines 74, 105, 129, 171 and 178; the OCR scrim is at ~444-447; the rate-fetch spinner is inside the result card at ~382.
    - `frontend/lib/core/theme/turista_theme.dart` — the `snackBarTheme` baseline (floating, 16px radius) established in 01-01, which these SnackBars inherit; only the background and text color are set locally.
    - `frontend/lib/core/theme/veltur_tokens.dart` — `danger`, `dangerSoft`, and the warm tint reachable through `shadowMd`.
    - `.planning/phases/01-redise-o-turista/01-UI-SPEC.md` — the Copywriting Contract row for the currency-converter error state, which pins the two l10n strings.
  </read_first>

  <action>
    **Loading.** Both `CircularProgressIndicator` instances currently inherit Flutter's default blue
    because neither sets a color. Give both `color: Theme.of(context).colorScheme.primary`. The
    full-screen OCR scrim's neutral wash becomes the warm shadow tint at the same opacity it uses today
    — read the tint through `VelturTokens.of(context)` rather than introducing a literal, so the scrim
    reads as warm dimming rather than a grey plate over a warm app.

    Do NOT introduce a connecting or reconnecting affordance anywhere on this screen. The UI-SPEC
    marks the only unresolved loading consideration in this phase as out of scope, and the two existing
    loading states here (`_isLoading` for OCR, `CurrencyLoading` for the rate fetch) are the complete
    set.

    **Errors.** All five `SnackBar` calls keep their existing message expressions verbatim — the l10n
    keys `invalidAmount`, `noNumberFound` and `error`, and the `'${l10n.error}: $e'` interpolation.
    Give each a `backgroundColor` of the `danger` token and a `contentTextStyle` from
    `textTheme.bodyLarge` colored for legibility on it. Do not restate the shape or the floating
    behaviour — both come from the theme.

    Because the same three-line treatment repeats five times, extract a small private helper on the
    state class — `void _showError(BuildContext context, String message)` — that builds and shows the
    themed `SnackBar`, and call it from all five sites. This keeps the five call sites from drifting
    apart and is the pattern the other screens in this phase will mirror.

    **Rate freshness.** If the screen currently displays the rate's timestamp or source, preserve it
    verbatim. If it does not, do NOT add one — inventing a freshness claim would be new copy, which
    this plan's third prohibition forbids. Instead, record in the SUMMARY whether a freshness cue
    exists, so the phase-wide human verification in 01-10 can judge the transparency prohibition with
    that fact in hand.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; flutter analyze 2>&amp;1 | grep -cE '^\s+error •' | grep -qx 0</automated>
    <automated>cd frontend &amp;&amp; ! (grep -hvE '^\s*(//|/\*|\*)' lib/core/tools/presentation/screens/currency_converter_screen.dart | grep -qE 'Colors\.(red|pink|purple|deepPurple|indigo|blue|lightBlue|cyan|teal|green|lightGreen|lime|yellow|amber|orange|deepOrange|brown|grey|blueGrey|black)|Color\(0x')</automated>
  </verify>

  <acceptance_criteria>
    - <!-- planner-discipline-allow: Colors\.(red|pink|purple|deepPurple|indigo|blue|lightBlue|cyan|teal|green|lightGreen|lime|yellow|amber|orange|deepOrange|brown|grey|blueGrey|black) -->
      <!-- planner-discipline-allow: Color\(0x -->
      `cd frontend && ! (grep -hvE '^\s*(//|/\*|\*)' lib/core/tools/presentation/screens/currency_converter_screen.dart | grep -qE 'Colors\.(red|pink|purple|deepPurple|indigo|blue|lightBlue|cyan|teal|green|lightGreen|lime|yellow|amber|orange|deepOrange|brown|grey|blueGrey|black)|Color\(0x')` succeeds.
    - `cd frontend && grep -c 'l10n.invalidAmount' lib/core/tools/presentation/screens/currency_converter_screen.dart` is exactly 1 and `grep -c 'l10n.noNumberFound' ...` is exactly 1 — both localised strings preserved.
    - `cd frontend && grep -c '_showError' lib/core/tools/presentation/screens/currency_converter_screen.dart` is at least 6 (the declaration plus five call sites).
    - `cd frontend && grep -c 'CircularProgressIndicator' lib/core/tools/presentation/screens/currency_converter_screen.dart` is exactly 2, and every occurrence is within 3 lines of a `color:` argument.
    - `git diff HEAD -- frontend/lib/core/tools/presentation/screens/currency_converter_screen.dart | grep -cE '^[-+].*(_convert|toStringAsFixed|_processImage|CurrencyCubit)'` is 0.
    - `cd frontend && flutter analyze` reports zero lines matching `error •`.
  </acceptance_criteria>

  <done>
    Scanning a price with the camera dims the screen in warm brown with a terracota spinner; entering
    an invalid amount raises a rounded terracota-red floating SnackBar carrying the same Spanish text
    as before.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| camera / gallery → OCR | Pre-existing. Untrusted image content reaches ML Kit text recognition. Unchanged by this plan. |
| exchange-rate API → displayed figure | Pre-existing. A remote number is rendered as financial guidance to the user. This plan changes how that number looks, not where it comes from. |

No credential, storage, auth or parsing surface changes here.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04-T | Tampering | conversion arithmetic and formatting in `currency_converter_screen.dart` | medium | mitigate | A restyle that drifted into `toStringAsFixed`, the symbol lookup or `_convert` would change a financial figure under cover of a visual commit. Both tasks carry a `git diff` gate asserting zero changed lines matching those identifiers. |
| T-01-04-R | Repudiation | rate freshness presentation | low | mitigate | Rendering a cached or demo rate with the visual authority of a live quote misleads a traveller. Not fixed here (adding a freshness cue would be new copy, forbidden by the third prohibition); instead Task 2 requires recording whether a freshness cue exists, so 01-10's human verification can judge it with evidence. |
| T-01-04-SC | Tampering | package installs | n/a | accept | This plan installs nothing. |
</threat_model>

<verification>
1. `cd frontend && flutter analyze` — zero `error •` lines.
2. Scoped literal-color gate over the screen — passes.
3. `! grep -q 'AppBorderRadius' ...` — passes.
4. Copy-preservation greps for `l10n.invalidAmount` and `l10n.noNumberFound` — each returns exactly 1.
5. `git diff` behaviour gates for the conversion and OCR identifiers — each returns 0.
6. Manual smoke (not a gate): convert an amount and confirm the figure is unchanged from a pre-restyle run of the same input.
</verification>

<success_criteria>
- Every radius on the converter comes from the Turista token scale.
- The result figure carries the Display role in terracota on a `primarySoft` card.
- Both spinners are terracota and the OCR scrim is warm-tinted.
- All five error SnackBars route through one themed helper and keep their localised strings.
- No conversion, rate-fetch or OCR line changed.
</success_criteria>

<artifacts_this_phase_produces>
| Symbol / file | Kind | Notes |
|---|---|---|
| `_showError(BuildContext, String)` | new private method | On the `CurrencyConverterScreen` state class; the themed-SnackBar helper other screens mirror |

No new files. No new public API.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-redise-o-turista/01-04-SUMMARY.md` when done. Record whether the screen
displays any exchange-rate freshness or source cue today (a direct input to 01-10's transparency
judgement), and the pre/post `AppSpacing` occurrence count proving spacing was untouched.
</output>
