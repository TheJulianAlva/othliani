---
phase: 01-redise-o-turista
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - frontend/lib/features/turista/home/presentation/widgets/walkie_talkie_button.dart
  - frontend/lib/features/turista/home/presentation/screens/comunicacion_seguridad_screen.dart
  - frontend/lib/features/turista/chat/presentation/screens/chat_screen.dart
  - frontend/lib/features/turista/chat/presentation/widgets/message_input_field.dart
  - frontend/test/features/turista/home/presentation/widgets/walkie_talkie_button_test.dart
autonomous: true
requirements: [DISENO-TUR-02, DISENO-TUR-01]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The push-to-talk button renders terracota when idle, danger red when transmitting and the warn amber when the channel is busy — three states that are unmistakably different at a glance (DISENO-TUR-02)."
    - "While transmitting, the button carries the shadowGlowDanger ring; while idle it carries shadowMd — the live-microphone state is signalled by both fill and halo, not by fill alone."
    - "The push-to-talk button exposes a Semantics label 'Mantén presionado para hablar' so the affordance is announced to screen readers, matching the visible label already present on the comunicación screen."
    - "Socket.IO event handlers, the recorder/player lifecycle, the streaming subscription and the onLongPress/onLongPressEnd callbacks in walkie_talkie_button.dart are unchanged — the restyle is confined to the build method's decoration."
    - "The SOS card, the walkie-talkie card and the channel-note callout on the comunicación screen use warm token colors, the 24px large radius and warm-tinted shadows instead of raw red/orange with neutral shadows."
    - "The channel-denied SnackBar keeps its existing Spanish text verbatim and is restyled with the warn/danger tokens (UI-SPEC error state, Walkie-talkie channel denied)."
    - "The chat empty state routes through EmptyStateWidget with the existing l10n string typeMessage ('Escribe un mensaje...') preserved verbatim, replacing the bare centred Text (UI-SPEC empty state, Chat message list)."
    - "flutter test test/features/turista/home/presentation/widgets/walkie_talkie_button_test.dart exits 0."
  artifacts:
    - frontend/test/features/turista/home/presentation/widgets/walkie_talkie_button_test.dart
  key_links:
    - "comunicacion_seguridad_screen.dart embeds WalkieTalkieButton inside a FittedBox sized 100x100 — the button's intrinsic 56px size is scaled up there, so any size change in the button silently rescales on this screen."
    - "chat_screen.dart must import EmptyStateWidget from core/widgets (made theme-driven in 01-01) — this is the first Turista consumer of that widget, which currently has none."
  prohibitions:
    - statement: "MUST NOT make the live-microphone state visually ambiguous. A user must be able to tell at a glance whether their voice is being transmitted to the group; a restyle that reduces the idle/recording distinction to a subtle hue shift is a privacy failure, not a style choice."
      category: privacy
      status: resolved
      verification: judgment
    - statement: "MUST NOT alter, translate or shorten the existing Spanish copy on these screens — including the SOS instructions, the walkie-talkie step list, the channel-enablement note and the l10n typeMessage string. Only typography, color and container chrome change."
      category: transparency
      status: resolved
      verification: judgment
    - statement: "MUST NOT weaken the SOS long-press guard into a tap or shorten its intent. The SOS control is an emergency affordance; making it easier to trigger while restyling would produce false alarms sent to a real guide."
      category: safety
      status: resolved
      verification: judgment
  assumptions:
    - "FLAGGED (edge-probe fallback, unresolved/unclassified): DISENO-TUR-02 came back `unclassified` from the deterministic edge probe — 'the walkie-talkie screen reflects the new design system' has no boundary/null/permission edge to classify, so no acceptance criterion was auto-derived. The planner substitutes the three-state colour/halo assertions above as the proxy; that substitution is an assumption, not a probe result."
    - "FLAGGED (UI-SPEC ## UI Considerations, loading / walkie-talkie socket connecting, status ⚠ unresolved): no connecting or reconnecting affordance exists today. Per the UI-SPEC's own note this phase INHERITS the assumption that the socket connects instantly and needs no loading UI. Do NOT invent a connecting state here."
    - "FLAGGED: the walkie-talkie instructions on comunicacion_seguridad_screen.dart say 'Mantén presionado el botón naranja'. After the restyle the button is terracota #E8623D, which still reads as an orange, so the copy stays accurate and is preserved verbatim per the copy prohibition. Recorded because a reviewer will otherwise flag it as stale."
    - "FLAGGED: the visible label 'Mantén presionado para hablar' ALREADY exists on comunicacion_seguridad_screen.dart. The UI-SPEC Copywriting Contract describes it as new copy; it is not. This plan therefore restyles the existing label and adds the same string as a Semantics label on the button widget, rather than introducing a second visible copy of it."
---

<objective>
Deliver DISENO-TUR-02: the walkie-talkie surface — the push-to-talk button, the comunicación y
seguridad screen that hosts it, and the chat screens next to it — reads as part of the warm system,
with the transmitting state more legible than it is today, and with the Socket.IO behaviour provably
untouched.

Purpose: this is one of the three screens the pitch video records. It is also the one where a purely
cosmetic change could do real harm — a user who cannot tell the microphone is live.

Output: a three-state push-to-talk button with a warm halo, two restyled cards, a chat empty state
that finally uses the shared widget, and a widget test pinning the three states.
</objective>

<execution_context>
@/home/the_julian_alva/Documentos/Projects/othliani/.claude/gsd-core/workflows/execute-plan.md
@/home/the_julian_alva/Documentos/Projects/othliani/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-redise-o-turista/01-CONTEXT.md
@.planning/phases/01-redise-o-turista/01-UI-SPEC.md
@.planning/phases/01-redise-o-turista/01-PATTERNS.md
@.planning/phases/01-redise-o-turista/01-01-SUMMARY.md
@.planning/sketches/002-turista-walkie-talkie/
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Botón push-to-talk — tres estados legibles con halo cálido</name>

  <files>
    frontend/lib/features/turista/home/presentation/widgets/walkie_talkie_button.dart,
    frontend/test/features/turista/home/presentation/widgets/walkie_talkie_button_test.dart
  </files>

  <read_first>
    - `frontend/lib/features/turista/home/presentation/widgets/walkie_talkie_button.dart` — 224 lines, full file. The socket handlers sit at roughly lines 38-81, the streaming logic at 144-176, `dispose` at 178-185, and the entire visual surface to change is the `build` method at 187-223.
    - `frontend/lib/core/theme/veltur_tokens.dart` — the `warn`, `danger`, `shadowMd`, `shadowGlowDanger` and `shadowGlowPrimary` fields.
    - `.planning/phases/01-redise-o-turista/01-UI-SPEC.md` — the shadow-glow token definitions and the walkie-talkie copy row.
    - `.planning/sketches/002-turista-walkie-talkie/` — the winning variant B, as design direction only (the real widget is a 56px circle embedded in a card, not the sketch's full-bleed hero).
    - `frontend/test/features/guia/shared/widgets/critical_medical_card_test.dart` — a pumped-widget test in this repo; note it currently FAILS on `main`, so scope test runs to the new file rather than the whole suite.
  </read_first>

  <behavior>
    - Test 1: with the channel free and not recording, the button's `BoxDecoration` fill is the terracota primary and its shadow list is the resting warm shadow.
    - Test 2: while recording, the fill is the danger token and the shadow list is the danger glow ring — both change together, so neither signal alone carries the live-microphone meaning.
    - Test 3: while the channel is busy, the fill is the warn token and the button is visibly non-primary.
    - Test 4: the widget exposes a `Semantics` node whose label is exactly `Mantén presionado para hablar`.
    - Test 5: the three state fills are pairwise unequal — the assertion that mechanically enforces the privacy prohibition above.
  </behavior>

  <action>
    Restyle ONLY the `build` method. Do not touch the socket event registration, the `_requestToSpeak`
    / `_stopStreaming` handlers, the recorder or player lifecycle, the microphone stream subscription,
    or `dispose`.

    **State colors.** Replace the three literal fills with tokens read through
    `VelturTokens.of(context)` and `Theme.of(context).colorScheme`: busy takes `warn`, recording takes
    `danger`, idle takes `colorScheme.primary`. The existing conditional structure
    (busy first, then recording, then idle) stays exactly as it is — only the three values change.

    **Halo.** Replace the two hand-rolled shadow lists: while recording use `shadowGlowDanger`, and
    otherwise use `shadowMd`. Both are warm-tinted, unlike the neutral drop shadow currently used for
    the idle state. The recording halo is the second, redundant signal that the microphone is live —
    it is the reason a color-blind user or a user glancing at a phone in sunlight can still tell.

    **Keep the circle.** `shape: BoxShape.circle`, the 56x56 size and the `Material(elevation:)`
    wrapper stay. The `elevation` values (12 recording / 6 idle) can stay as they are — they are a
    Material shape affordance rather than a palette artefact, and the comunicación screen scales this
    widget through a `FittedBox`, so changing its intrinsic size would rescale that screen.

    **Icon.** The `Icons.radio` glyph and its 28px size stay; keep its foreground white, which reads
    correctly on all three token fills.

    **Semantics.** Wrap the existing `GestureDetector` in a `Semantics` node with
    `label: 'Mantén presionado para hablar'` and `button: true`, without altering `onLongPress` or
    `onLongPressEnd`. This string already appears as a visible label on
    `comunicacion_seguridad_screen.dart`; adding it here makes the affordance reachable when the button
    is used from any other host and announces it to assistive tech.

    **Channel-denied SnackBar.** The handler that reports a busy channel keeps its Spanish text
    verbatim. Give the `SnackBar` a `backgroundColor` of the `warn` token and a `contentTextStyle`
    colored for legibility on it; the floating shape and 16px radius come from the theme's
    `snackBarTheme` established in 01-01, so do not restate them here.

    **Tests.** Create the test file with the five behaviors. Drive the three states by pumping the
    widget and manipulating its state rather than by standing up a real socket — if the widget's state
    fields are private and not reachable, extract the decoration into a small pure
    `static BoxDecoration decorationFor({required bool isRecording, required bool isChannelBusy, required VelturTokens tokens, required ColorScheme scheme})`
    helper on the widget class and test that directly. Prefer the helper: it makes the three-state
    contract testable without a socket, and the `build` method then reads as a single call to it.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; flutter test test/features/turista/home/presentation/widgets/walkie_talkie_button_test.dart</automated>
    <automated>cd frontend &amp;&amp; flutter analyze 2>&amp;1 | grep -cE '^\s+error •' | grep -qx 0</automated>
  </verify>

  <acceptance_criteria>
    - `cd frontend && flutter test test/features/turista/home/presentation/widgets/walkie_talkie_button_test.dart` exits 0 with 5+ passing tests.
    - `git diff HEAD -- frontend/lib/features/turista/home/presentation/widgets/walkie_talkie_button.dart | grep -cE '^[-+].*(socket\.|_recorder|_player|_micSubscription|onLongPress|_requestToSpeak|_stopStreaming|emit\(|\.on\()'` is 0 — every changed line is visual.
    - <!-- planner-discipline-allow: Colors\.(red|pink|purple|deepPurple|indigo|blue|lightBlue|cyan|teal|green|lightGreen|lime|yellow|amber|orange|deepOrange|brown|grey|blueGrey|black) -->
      `cd frontend && ! (grep -hvE '^\s*(//|/\*|\*)' lib/features/turista/home/presentation/widgets/walkie_talkie_button.dart | grep -qE 'Colors\.(red|pink|purple|deepPurple|indigo|blue|lightBlue|cyan|teal|green|lightGreen|lime|yellow|amber|orange|deepOrange|brown|grey|blueGrey|black)')` succeeds.
    - `cd frontend && grep -c 'Mantén presionado para hablar' lib/features/turista/home/presentation/widgets/walkie_talkie_button.dart` is exactly 1.
    - Test 5 asserts the three fills are pairwise unequal and passes.
    - `cd frontend && flutter analyze` reports zero lines matching `error •`.
  </acceptance_criteria>

  <done>
    Holding the button turns it unmistakably red with a soft red halo; releasing returns it to
    terracota with a resting warm shadow; a busy channel shows amber. The Socket.IO round trip works
    exactly as it did before the change.
  </done>
</task>

<task type="auto">
  <name>Task 2: Pantalla de comunicación y seguridad — tarjetas SOS y radio en tokens cálidos</name>

  <files>
    frontend/lib/features/turista/home/presentation/screens/comunicacion_seguridad_screen.dart
  </files>

  <read_first>
    - `frontend/lib/features/turista/home/presentation/screens/comunicacion_seguridad_screen.dart` — 204 lines, full file. It already reads `theme.cardColor`, `theme.colorScheme.primary` and `theme.textTheme` in places; the literal-color sites are the SOS card border/shadow/icon/title/button (lines ~38-104), the channel-note callout (lines ~152-172) and the hold-to-talk caption (line ~192).
    - `frontend/lib/core/theme/veltur_tokens.dart` — `danger`, `dangerSoft`, `warn`, `warnSoft`, `textMuted`, `border`, `radiusLg`, `radiusXl`, `radiusFull`, `shadowSm`, `shadowMd`.
    - `frontend/lib/core/theme/app_constants.dart` — `AppSpacing` (unchanged this phase) and `AppBorderRadius` (must NOT be used for new radii — the 24px value here must come from the token extension, since `AppBorderRadius.lg` is 16 and belongs to Agencia).
    - `.planning/phases/01-redise-o-turista/01-UI-SPEC.md` — the Color role table; note the destructive role is reserved for exactly this kind of emergency surface.
  </read_first>

  <action>
    Restyle the screen's chrome. Every Spanish string on this screen — the heading, the SOS
    instructions paragraph, the `MANTENER PRESIONADO (SOS)` button label, the three-step walkie-talkie
    instructions including the reference to the orange button, the channel-enablement note and the
    `Mantén presionado para hablar` caption — is preserved character for character.

    **SOS card.** Swap the raw red border, tinted shadow and icon for the `danger` token, and give the
    card `dangerSoft` as a background wash instead of the plain card color so the emergency section
    reads as a distinct zone. Its corner radius moves from `AppBorderRadius.lg` (16, the Agencia scale)
    to the token `radiusLg` (24). Replace the hand-rolled shadow with `shadowSm`, tinted warm.

    **SOS button.** The 80px-tall pill keeps `borderRadius: 40` — that is already effectively the full
    radius for its height, so express it as `radiusFull` clamped by the shape rather than a literal.
    Its fill becomes the `danger` token (dropping the darker `shade700` variant, which has no token
    equivalent and would drift from the palette), its halo becomes `shadowGlowDanger`, and its label
    keeps white text at its current size, weight and letter spacing. The `onLongPress` handler, the
    demo-mode branch, the `DemoSocketService.instance.emitPanic()` call and the route push are
    untouched — the long-press guard is protected by this plan's third prohibition. The fallback
    `SnackBar` keeps its Spanish text and takes the `danger` token as its background.

    **Walkie-talkie card.** It already derives its border and shadow from `theme.colorScheme.primary`,
    so it inherits terracota automatically; only its corner radius needs to move from
    `AppBorderRadius.lg` to the token `radiusLg` (24), and its hand-rolled shadow to `shadowSm`.

    **Channel-note callout.** Replace the raw orange fill, border and icon/text color with the `warn`
    and `warnSoft` tokens, and move its 8px corner radius to `radiusSm` (10). Give its text
    `Theme.of(context).textTheme.labelLarge` rather than the local 12px/w600 literal, so the type scale
    holds.

    **Hold-to-talk caption.** Replace the neutral grey 12px literal with
    `Theme.of(context).textTheme.labelLarge` recolored to the `textMuted` token.

    **Heading.** Drop the local `fontWeight: FontWeight.bold` override on the screen heading — the
    theme's `titleLarge`/`headlineSmall` already carries weight 600 and the UI-SPEC caps the app at two
    weights.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; flutter analyze 2>&amp;1 | grep -cE '^\s+error •' | grep -qx 0</automated>
    <automated>cd frontend &amp;&amp; ! (grep -hvE '^\s*(//|/\*|\*)' lib/features/turista/home/presentation/screens/comunicacion_seguridad_screen.dart | grep -qE 'Colors\.(red|pink|purple|deepPurple|indigo|blue|lightBlue|cyan|teal|green|lightGreen|lime|yellow|amber|orange|deepOrange|brown|grey|blueGrey|black)|Color\(0x')</automated>
  </verify>

  <acceptance_criteria>
    - <!-- planner-discipline-allow: Colors\.(red|pink|purple|deepPurple|indigo|blue|lightBlue|cyan|teal|green|lightGreen|lime|yellow|amber|orange|deepOrange|brown|grey|blueGrey|black) -->
      <!-- planner-discipline-allow: Color\(0x -->
      `cd frontend && ! (grep -hvE '^\s*(//|/\*|\*)' lib/features/turista/home/presentation/screens/comunicacion_seguridad_screen.dart | grep -qE 'Colors\.(red|pink|purple|deepPurple|indigo|blue|lightBlue|cyan|teal|green|lightGreen|lime|yellow|amber|orange|deepOrange|brown|grey|blueGrey|black)|Color\(0x')` succeeds.
    - Every one of these exact strings still appears exactly once in the file: `Herramientas de Seguridad y Comunicación`, `Botón de Emergencia (SOS)`, `MANTENER PRESIONADO (SOS)`, `Radio Grupal (Walkie-Talkie)`, `Mantén presionado para hablar`. Verify with `grep -c` per string.
    - `cd frontend && grep -c 'Mantén presionado el botón naranja' lib/features/turista/home/presentation/screens/comunicacion_seguridad_screen.dart` is exactly 1 — the instruction copy is preserved verbatim despite the palette change.
    - `git diff HEAD -- frontend/lib/features/turista/home/presentation/screens/comunicacion_seguridad_screen.dart | grep -cE '^[-+].*(onLongPress|emitPanic|context\.push|kDemoMode)'` is 0 — the SOS trigger path is untouched.
    - <!-- planner-discipline-allow: AppBorderRadius -->
      `cd frontend && ! grep -q 'AppBorderRadius' lib/features/turista/home/presentation/screens/comunicacion_seguridad_screen.dart` succeeds — radii now come from the Turista token scale, not the Agencia one.
    - `cd frontend && flutter analyze` reports zero lines matching `error •`.
  </acceptance_criteria>

  <done>
    The comunicación screen shows a warm-washed emergency card in the danger token, a terracota radio
    card, and an amber channel note — all on 24px corners with soft warm shadows, with every Spanish
    instruction intact and the SOS long-press behaving exactly as before.
  </done>
</task>

<task type="auto">
  <name>Task 3: Chat — estado vacío por EmptyStateWidget y campo de mensaje en tokens</name>

  <files>
    frontend/lib/features/turista/chat/presentation/screens/chat_screen.dart,
    frontend/lib/features/turista/chat/presentation/widgets/message_input_field.dart
  </files>

  <read_first>
    - `frontend/lib/features/turista/chat/presentation/screens/chat_screen.dart` — 57 lines, full file. The empty branch is the ternary at lines 39-41.
    - `frontend/lib/features/turista/chat/presentation/widgets/message_input_field.dart` — 49 lines, full file.
    - `frontend/lib/core/widgets/empty_state_widget.dart` — as rewritten in 01-01; its constructor takes `icon` and `message` only.
    - `frontend/lib/features/turista/chat/presentation/widgets/chat_bubble.dart` — already converted in 01-01; do not modify it here.
    - `.planning/phases/01-redise-o-turista/01-UI-SPEC.md` — the Copywriting Contract row for the chat empty state.
  </read_first>

  <action>
    **`chat_screen.dart`.** Replace the bare centred `Text(l10n.typeMessage)` in the empty branch with
    `EmptyStateWidget(icon: Icons.forum_outlined, message: l10n.typeMessage)`. The l10n key and its
    resolved Spanish string `Escribe un mensaje...` are preserved exactly — this is a container and
    typography change, not a copy change. This is the first Turista consumer of `EmptyStateWidget`,
    which currently has none; wiring it here is what makes the widget reachable rather than dead code.
    Leave the `ListView.builder`, the `_messages` state, `_sendMessage` and the `dispose` override
    untouched. Give the list `padding` the existing 16 value — it already matches `AppSpacing.md`.

    **`message_input_field.dart`.** Read the file first: it declares no literal colors today, so it may
    already inherit correctly from the theme. Confirm whether its send control and text field derive
    from `Theme.of(context)`; if either states a border, fill, radius or icon color locally, replace it
    with the theme/token equivalent — the field takes the theme's `inputDecorationTheme` (10px radius,
    warm border, terracota focus) and the send affordance takes `colorScheme.primary` with
    `radiusFull`. If the file genuinely needs no change, record that finding in the SUMMARY rather than
    editing it to look busy.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; flutter analyze 2>&amp;1 | grep -cE '^\s+error •' | grep -qx 0</automated>
    <automated>cd frontend &amp;&amp; grep -c 'EmptyStateWidget' lib/features/turista/chat/presentation/screens/chat_screen.dart | grep -qE '^[2-9]'</automated>
  </verify>

  <acceptance_criteria>
    - `cd frontend && grep -c 'EmptyStateWidget' lib/features/turista/chat/presentation/screens/chat_screen.dart` is at least 2 (the import plus the usage).
    - `cd frontend && grep -c 'l10n.typeMessage' lib/features/turista/chat/presentation/screens/chat_screen.dart` is exactly 1 — the existing localised string is reused, not replaced by a literal.
    - <!-- planner-discipline-allow: Colors\.(red|pink|purple|deepPurple|indigo|blue|lightBlue|cyan|teal|green|lightGreen|lime|yellow|amber|orange|deepOrange|brown|grey|blueGrey|black) -->
      <!-- planner-discipline-allow: Color\(0x -->
      `cd frontend && ! (grep -hvE '^\s*(//|/\*|\*)' lib/features/turista/chat/presentation/screens/chat_screen.dart lib/features/turista/chat/presentation/widgets/message_input_field.dart | grep -qE 'Colors\.(red|pink|purple|deepPurple|indigo|blue|lightBlue|cyan|teal|green|lightGreen|lime|yellow|amber|orange|deepOrange|brown|grey|blueGrey|black)|Color\(0x')` succeeds.
    - `git diff HEAD -- frontend/lib/features/turista/chat/presentation/screens/chat_screen.dart | grep -cE '^[-+].*(_sendMessage|_messages|_controller|dispose)'` is 0 — chat behaviour untouched.
    - `cd frontend && flutter analyze` reports zero lines matching `error •`.
  </acceptance_criteria>

  <done>
    An empty chat shows a warm teal icon above the existing "Escribe un mensaje..." caption in muted
    warm type instead of a bare grey line, and the message field matches every other input in the app.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device microphone → Socket.IO group channel | Pre-existing. Audio leaves the device and reaches every group member. This plan does not change the mechanism, but it does change the only UI signal a user has that the boundary is open. |
| user → SOS emergency dispatch | Pre-existing. A long press reaches a real guide and, in the production design, agency authorities. |

No network endpoint, credential, storage path or parser changes in this plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03-I | Information Disclosure | `walkie_talkie_button.dart` transmitting-state affordance | high | mitigate | A restyle that makes idle and recording read alike lets a user speak while believing the microphone is closed — voice leaks to the whole group. Mitigated by two independent redundant signals (fill token AND halo token change together) plus Test 5, which fails the build if any two of the three state fills are equal. This is the one high-severity item in the phase and the reason the button carries a dedicated test file. |
| T-01-03-T | Tampering | Socket.IO handlers in `walkie_talkie_button.dart` | medium | mitigate | A visual pass that drifted into the event handlers could change what is emitted or when. Task 1's second acceptance criterion is a `git diff` gate asserting zero changed lines matching the socket, recorder, player, subscription and long-press identifiers. |
| T-01-03-D | Denial of Service | SOS long-press guard on `comunicacion_seguridad_screen.dart` | medium | mitigate | Turning the long press into a tap during a restyle would generate false emergency dispatches to a real guide. Task 2's fourth acceptance criterion is a `git diff` gate on `onLongPress` / `emitPanic` / the route push, and the third prohibition records the intent. |
| T-01-03-SC | Tampering | package installs | n/a | accept | This plan installs nothing. |
</threat_model>

<verification>
1. `cd frontend && flutter test test/features/turista/home/presentation/widgets/walkie_talkie_button_test.dart` — exits 0.
2. `cd frontend && flutter analyze` — zero `error •` lines.
3. Scoped literal-color gate over all four modified files — passes.
4. Copy-preservation greps for the six named Spanish strings — each returns exactly 1.
5. Manual smoke (not a gate): run Turista in demo mode, open Comunicación y Seguridad, hold the radio button and confirm the red fill plus red halo appear together and disappear together on release.
</verification>

<success_criteria>
- The push-to-talk button has three unmistakably distinct states, each proven by a passing assertion.
- The comunicación screen reads warm end to end with every Spanish instruction preserved.
- The chat empty state uses the shared widget rather than a bare `Text`.
- Socket.IO and SOS behaviour are provably unchanged by `git diff` gates, not by assertion in prose.
</success_criteria>

<artifacts_this_phase_produces>
| Symbol / file | Kind | Notes |
|---|---|---|
| `frontend/test/features/turista/home/presentation/widgets/walkie_talkie_button_test.dart` | new file | 5+ tests |
| `WalkieTalkieButton.decorationFor(...)` | new static method (conditional) | Only if the private-state route proves untestable; see Task 1 |
| First Turista call site of `EmptyStateWidget` | new wiring | `chat_screen.dart` |
| Semantics label `Mantén presionado para hablar` on `WalkieTalkieButton` | new a11y surface | The visible label already existed on the comunicación screen |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-redise-o-turista/01-03-SUMMARY.md` when done. Record whether the
`decorationFor` helper was needed, and whether `message_input_field.dart` required any change at all.
</output>
